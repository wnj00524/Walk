# T019 evidence

## Batch and provenance

The finite local budget authorized exactly `rock_temperate_01`,
`tree_temperate_01`, and `groundcover_fern_01`. Meshy accepted all three
preview requests and each existing task was resumed to `SUCCEEDED`; no request
was retried. Verified GLBs and technical reports are retained under the
ignored `asset_work/candidates/` and `asset_work/` paths. Credentials, signed
URLs, and raw service responses were not committed.

Python inspection measured 906,204 bytes / 31,189 indexed triangles for the
rock, 2,088,272 bytes / 31,005 triangles for the tree, and 1,791,660 bytes /
30,135 triangles for the fern. The catalogue remains empty because these
models have not passed owner visual acceptance.

## Checks and blocker

- `python tools/asset_registry.py` — exit 0; request and empty catalogue valid.
- `python tools/import_asset.py ...` — exit 0 for all three GLBs; reports retained locally.
- Godot import fixture — each asset produced one mesh instance and measured bounds; Godot also emitted renderer cleanup/leak diagnostics on headless exit.
- `python tools/project.py validate` — exit 0; 37 task records checked.
- `python -m unittest discover -s tools -p "test_*.py"` — exit 0, 51 tests passed.

T019 is BLOCKED under the task's stop condition: fixed close/mid/far views in
the game lighting and the owner's ACCEPT/REVISE decision are not yet
available. No technical pass is being represented as aesthetic approval.
