# T024a evidence — vegetation grouping revision

## Implementation

T024a raised the bounded grouping budgets in `game/data/habitats.json` and
added an independent placement assertion covering density, cluster, and total
budget limits. The placement runner now executes seven focused checks.

## Checks

- `python tools/check_game.py --suite placement` — PASS, 7/7.
- `python tools/check_game.py --suite vegetation` — PASS, 5/5.
- `python tools/check_game.py --suite visual_preset` — PASS, 5/5.
- `python tools/run_visual.py --output-dir artifacts/T024a_visual_capture --timeout 180` — PASS, four PNGs and a report retained.

The refreshed report records 299 rendered instances, nine resident batches,
world seed 42, and no vegetation runtime error. The PNGs still do not provide
readable forest/meadow/rock grouping: the approved meshes appear as sparse
small white/grey fragments at the fixed route viewpoints.

## Decision

BLOCKED. T024a is data/test scoped and explicitly forbids renderer or capture
route changes. Investigation found that `VegetationBatches.apply_batch` keeps
records in local cell coordinates but does not place each batch at its cell
world origin. Correcting that would be a separate renderer task, so it was not
included in T024a. T024 remains blocked pending that scoped follow-up and the
owner visual gate.
