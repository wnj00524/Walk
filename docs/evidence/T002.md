# T002 Evidence: Create a minimal Godot project shell

Date: 16 September 2026  
Role: implementer

## Outcome

Created a minimal Godot 4.6.2 project that opens a 960 by 540 prototype screen, visibly identifies itself as unfinished, and exits with code 0 after a real graphical window-close request. The scene uses built-in controls only and adds no terrain, movement, menus, plugins, or runtime network calls.

## Verification commands and actual results

| Check | Command | Exit code | Result / retained evidence |
| --- | --- | ---: | --- |
| Pinned editor identity | `/tmp/walk-toolchain/Godot_v4.6.2-stable_linux.x86_64 --version` | 0 | `4.6.2.stable.official.71f334935`; output in `artifacts/T002/godot-version.log` |
| Headless resource import | `/tmp/walk-toolchain/Godot_v4.6.2-stable_linux.x86_64 --headless --path game --editor --quit` | 0 | Project initialization and file scan completed with no errors; full output in `artifacts/T002/headless-import.log` |
| Graphical launch and close | Xvfb/Openbox launch of `/tmp/walk-toolchain/Godot_v4.6.2-stable_linux.x86_64 --path game`, followed by `wmctrl -c 'Endless Nature'` | 0 | Title and status were visually inspected; screenshot at `artifacts/T002/project-shell.png`, engine output at `artifacts/T002/graphical-launch.log` |
| Plan validation | `python tools/project.py validate` | 0 | `PASS: 36 task records and plan structure checked. Game/runtime NOT TESTED.`; output in `artifacts/T002/project-validate.log` |
| Helper regression suite | `python -m unittest discover -s tools -p 'test_*.py'` | 0 | 25 tests ran in 2.183 seconds, all passed; output in `artifacts/T002/helper-tests.log` |

The graphical runner used Xvfb, Openbox, and Mesa llvmpipe because the container has no physical display or GPU. Godot warned that V-Sync could not be changed and that no audio driver was available; neither warning prevented this silent shell from rendering or closing normally. The project screenshot was checked after the Godot splash screen had cleared.

## Documentation

- `docs/features/project-shell.md` explains what appears, how startup and shutdown work, safe cosmetic changes, failure symptoms, and launch commands.
- `docs/CODE_GUIDE.md` links the three new runtime files to their responsibilities and checks.
- Each authored runtime file begins with a plain-English purpose, inputs/outputs, safe-change boundary, and failure description.

## Known limitations and next step

- This Linux software-rendered check does not prove Windows execution, GPU performance, audio output, or final visual quality.
- T003 should add a tiny game test runner that demonstrably reports both passing and failing tests after T002 is reviewed and accepted.
