# T005 evidence - comfortable basic first-person movement

## Result

Implemented a Godot `CharacterBody3D` walker with named WASD actions, metre/second
movement, bounded mouse look, cursor release/re-capture, and a labelled simple test
floor. The fixture explicitly says that terrain is not part of this check.

## Commands and results

Run from `C:\Users\jdwil\source\repos\Codex\Walk` on 2026-09-17:

| Command | Exit | Result |
| --- | ---: | --- |
| `godot --headless --path game --editor --quit-after 2` | 0 | Project imported successfully on Godot 4.6.2 |
| `python tools/check_game.py --suite player` | 0 | `executed=3 passed=3 failed=0` |
| `python tools/check_game.py --suite smoke` | 0 | `executed=1 passed=1 failed=0` |
| `python tools/project.py validate` | 0 | 36 task records and plan structure checked; game/runtime not tested by this helper |
| `python -m unittest discover -s tools -p "test_*.py"` | 0 | 30 tests passed |

Retained game-check logs are under `artifacts/`, including the player run at
`check_game_20260917-145054_import.log` and
`check_game_20260917-145054_suite_player.log`.

## Graphical check

The project was launched with `godot --path game` using the real NVIDIA GeForce RTX
2060 OpenGL compatibility renderer. The visible window showed the daylight floor,
start marker, controls, and the explicit test-floor label. Mouse look changed the
view, Escape released the cursor, and clicking re-captured it. A W input was accepted
without a runtime error. This is a manual fixture check only; it does not certify
terrain, long-distance continuity, sustained performance, audio, or a Windows export.

## Documentation and scope

The behaviour is explained in `docs/features/walking.md` and indexed in
`docs/CODE_GUIDE.md`. T005 stays in REVIEW for a separate review pass; only the
coordinator promotes it to DONE.
