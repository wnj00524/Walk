# T016 evidence - offline asset request registry

## Environment

- Python: `3.12.12`
- Offline validation; no Meshy or other provider calls

## Checks

| Command | Result | Retained evidence |
| --- | --- | --- |
| `python tools/asset_registry.py` | PASS; 3 request records, empty approved catalogue, zero-job budget | terminal result |
| `python -m unittest tools/test_asset_registry.py` | PASS, 7 tests | terminal result |
| `python tools/project.py validate` | PASS, 37 task records | terminal result |
| `python -m unittest discover -s tools -p "test_*.py"` | PASS, 40 tests | terminal result |

## Coverage

The validator rejects duplicate IDs, invalid lifecycle states, mismatched
prompt/config hashes, traversal paths, missing approved files, and enabled
budgets without named positive limits. The three examples are REQUESTED only;
the shipped catalogue is empty and no runtime placeholder is added.

## Review status

The task is ready for review. No provider network request, credential use, or
paid generation occurred.
