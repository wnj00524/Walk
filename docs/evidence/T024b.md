# T024b evidence — vegetation batch world placement

The renderer now assigns each batch the owning cell origin `(cell_x * 256, 0,
cell_z * 256)` while leaving records in local half-open coordinates. The focused
test uses cell `(2, -3)` and asserts the resulting origin `(512, 0, -768)`.

Checks:

- `python tools/check_game.py --suite vegetation` — PASS, 5/5.
- `python tools/run_visual.py --output-dir artifacts/T024a_visual_capture --timeout 180` — PASS, four captures retained.

Decision: BLOCKED. The world-placement correction passes, but isolated review
captures still show the approved tree, rock, and fern as solid white geometry.
That material-binding defect is outside T024b and is split into T024c.
