# T012 - verification record

## Gate result

The pinned Voxel Tools 1.7 foundation is rejected for the current product
gate. This is a completed review decision, not a claim that the local probe was
useless: T008 proved Windows extension loading and reflected API calls, and T009
produced a graphical local smooth-terrain probe. Those results do not satisfy
the foundation criteria.

The required criteria fail as follows:

| Criterion | Result | Evidence |
| --- | --- | --- |
| Windows exported prototype launches | NOT RUN / unverified | `docs/toolchain.lock.json`: export templates not tested; no retained export launch evidence |
| Terrain and collision stream | NOT PROVEN | T009 headless probe exit 1; native async completion was not reported and exact resident mesh/collision counts were unavailable |
| Coordinate strategy preserves identity | FAILS tested candidate | T010 recorded 0.03125 m `Vector3` precision loss at ±1,000,000.03125 m and a 6.926433 m sample delta after origin reset without a native generator offset |
| No manual scene editing or engine compilation | NOT ACCEPTANCE-SUFFICIENT | The project is text-defined, but this criterion cannot compensate for the failed runtime gates |

The pinned candidate remains available as a comparison probe only. The lock file
now records `status: REJECTED_BY_T012` and keeps `checks.backend_gate: false`.
Dependent terrain work must stop. A separate bounded alternative-backend spike
must be approved before changing C03 or making later terrain-dependent tasks
READY; no finite-map substitute is authorized.

## Checks

| Command | Exit code | Result |
| --- | ---: | --- |
| `python tools/project.py validate` | 0 | PASS: 36 task records and blocked dependency progression validated |
| `python -m unittest discover -s tools -p "test_*.py"` | 0 | PASS: 30 tests |

## Documentation

Updated `docs/toolchain.lock.json`, `docs/DECISIONS.md`,
`docs/features/terrain-api.md`, `docs/CODE_GUIDE.md`, `PLAN.md`, and `DLOG.md`.
Updated `tools/test_project.py` so the state-progression regression asserts the
intentional no-dispatch condition after a blocked foundation gate. No
production source, contract, world/save format, or game expectation was
changed.

## Handoff

T012 is BLOCKED with the backend gate false. There is no READY task after this
decision; the next work requires a coordinator-approved alternative terrain
spike or an explicit plan change.
