# T003 evidence - tiny test runner

## Result

Implemented the named Godot test runner and kept the intentionally failing fixture out of the normal smoke suite. Submitted for review; PLAN.md remains the status authority.

## Commands and results

Pinned Godot 4.6.2 (`4.6.2.stable.official.71f334935`) was run from the repository root:

| Command | Exit | Result |
| --- | ---: | --- |
| `godot --headless --path game --script res://tests/run_tests.gd -- --suite smoke` | 0 | `executed=1 passed=1 failed=0` |
| `godot --headless --path game --script res://tests/run_tests.gd -- --suite missing` | 1 | unknown suite rejected |
| `godot --headless --path game --script res://tests/run_tests.gd -- --suite ""` | 1 | empty suite rejected |
| `godot --headless --path game --script res://tests/run_tests.gd -- --self-test failure` | 1 | intentional failure propagated |
| `python tools/project.py validate` | 0 | `36 task records and plan structure checked` |
| `python -m unittest discover -s tools -p "test_*.py"` | 0 | helper suite completed successfully; 25 tests passed |

## Limitations

The checks are headless and offline. They do not demonstrate visual quality, audio, GPU performance, or any later feature.

## Follow-up verification on Windows

| Area | Result | Evidence |
| --- | --- | --- |
| Graphical launch | PASS | `godot --path game --quit-after 3 --verbose` exited 0. The application initialized the native OpenGL compatibility renderer. A screenshot/visual-quality review was not performed. |
| Renderer hardware | PASS for initialization only | Godot reported NVIDIA GeForce RTX 2060, OpenGL 3.3.0, driver 546.17. The shell has no 3D workload, so this is not a performance benchmark. |
| Audio | NOT RUN | The project contains 0 `.wav`, `.ogg`, or `.mp3` files and 0 audio nodes. There is no sound path to exercise. |
| GPU performance | NOT RUN | No terrain, walker, or 3D scene exists; no meaningful frame-time workload can be measured. |
| Windows export | BLOCKED | `godot --headless --path game --export-release "Windows Desktop" artifacts/T003_probe.exe` exited 1: no `export_presets.cfg` exists. The recorded export templates are also untested. |
