# T004 evidence - reproducible game-check command

## Result

Implemented `tools/check_game.py`, which verifies the pinned Godot candidate,
imports the project before running a selected registered suite, rejects engine
or script errors even when a process exits 0, requires nonzero executed counts,
and saves phase output under `artifacts/`.

## Commands and results

Run from `C:\Users\jdwil\source\repos\Codex\Walk` on 2026-09-17:

| Command | Exit | Result |
| --- | ---: | --- |
| `python -m unittest tools/test_check_game.py -v` | 0 | 5 tests passed |
| `python tools/check_game.py --suite smoke` | 0 | `executed=1 passed=1 failed=0`; import completed first |
| `python tools/project.py validate` | 0 | 36 task records and plan structure checked |
| `python -m unittest discover -s tools -p "test_*.py"` | 0 | 30 tests passed |

## Retained evidence

The real smoke invocation wrote:

- `artifacts/check_game_20260917-143830_version.log`
- `artifacts/check_game_20260917-143830_import.log`
- `artifacts/check_game_20260917-143830_suite_smoke.log`

The unit tests cover missing binary, timeout, exit-0 script error, unknown
suite, and success with an actual count using fake subprocess results only.

## Limitations

The command is offline and headless. It does not prove visual quality, audio,
GPU performance, Windows packaging, or later walking behaviour.
