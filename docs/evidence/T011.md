# T011 - verification record

## Result

The terrain adapter boundary is frozen for downstream design, with an explicit
non-acceptance decision for the current large-world backend. The contract names
the configure, viewer, readiness, surface, diagnostics, and shutdown operations;
their states, generation-epoch rule, main-thread boundary, seed mapping, and
comparison tolerances are recorded in `docs/CONTRACTS.md`.

The pinned Voxel Tools 1.7 evidence supports the local probe calls used by T009:
`VoxelLodTerrain`, `VoxelGeneratorNoise`, `VoxelMesherTransvoxel`, and
`VoxelViewer`, including collision configuration and backend statistics. T010
does not support accepting a visual-origin handoff: `Vector3` precision exceeded
the 0.01 m threshold at the tested ±1,000,000.03125 m points, and the
origin-move candidate changed the sampled height by 6.926433 m.

## Contract disposition

The exact adapter shape is frozen, but the current `TerrainService` remains a
probe implementation with temporary methods. T012 must stay WAITING until a
real adapter or separately approved alternative spike proves mesh completion,
collision continuity, exported-build behaviour, and identity-preserving travel.
The service must report `OUTSIDE_TESTED_SUPPORT` rather than silently recentering
when the native backend cannot preserve identity.

## Checks

| Command | Exit code | Result |
| --- | ---: | --- |
| `python tools/project.py validate` | 0 | PASS: 36 task records and plan structure checked |
| `python -m unittest discover -s tools -p "test_*.py"` | 0 | PASS: 30 tests |
| `python tools/check_game.py --suite smoke` | 0 | PASS: executed=1 passed=1 failed=0 |
| `python tools/check_game.py --suite player` | 0 | PASS: executed=3 passed=3 failed=0 |
| `python tools/check_game.py --suite coordinates` | 0 | PASS: executed=4 passed=4 failed=0 |
| `python tools/check_game.py --suite deterministic` | 0 | PASS: executed=3 passed=3 failed=0 |

## Documentation

Updated `docs/CONTRACTS.md`, `docs/features/terrain-api.md`,
`docs/DECISIONS.md`, and `docs/CODE_GUIDE.md`. Updated the helper's state-
progression assertions in `tools/test_project.py`. No production source, native
backend, world/save format, or game expectation was changed.

## Unverified / blocked

- The frozen adapter methods are not implemented by the current probe.
- Native mesh/collision completion in a bounded headless run remains unverified.
- No identity-preserving native origin offset or accepted alternative backend exists.
- Windows export and long-distance continuous collision remain unverified.

## Handoff

Review passed: verified frozen terrain adapter contract (C03 typed signatures,
explicit states, generation epochs, main-thread boundary, seed mapping, and
tolerances), non-acceptance decision for large-world recentering, and regression
suites. State progression updated: T011 DONE, T012 READY.
