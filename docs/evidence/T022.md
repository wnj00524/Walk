# T022 evidence - bounded vegetation batches

## Result

Added a main-thread vegetation renderer that consumes deterministic placement
records, keeps at most nine logical-cell batches, releases cells outside the
three-by-three neighborhood, and rejects stale epochs, invalid records, and
unapproved assets without fallback geometry. The main walker now loads the
habitats/catalogue and updates the renderer with the same logical position as
the terrain. Three retextured Meshy derivatives now have approved catalogue
records and are loaded as runtime GLBs.

## Checks

| Command | Exit | Result |
| --- | ---: | --- |
| `python tools/check_game.py --suite vegetation` | 0 | 5 executed, 5 passed, 0 failed, including real catalogue/GLB integration |
| `python tools/check_game.py --suite placement` | 0 | 6 executed, 6 passed, 0 failed |
| `python tools/check_game.py --suite deterministic` | 0 | 3 executed, 3 passed, 0 failed |
| `python tools/check_game.py --suite terrain` | 0 | 4 executed, 4 passed, 0 failed |
| `python tools/check_game.py --suite chunk_terrain` | 0 | 3 executed, 3 passed, 0 failed |
| `python tools/check_game.py --suite smoke` | 0 | 1 executed, 1 passed, 0 failed |
| `python -m unittest discover -s tools -p "test_*.py"` | 0 | 51 tests passed |
| `python tools/project.py validate` | 0 | 37 task/plan checks passed; runtime not tested by this command |
| `git diff --check` | 0 | no whitespace errors |
| `python tools/asset_registry.py` | 0 | approved catalogue and budget validated; 3 requests |
| `python tools/run_visual.py --output-dir artifacts/T022_visual_capture --timeout 180` | 0 | 4 real-renderer captures and report retained |

## Visual gate

PASS for the T022 integration gate on Windows/OpenGL with an NVIDIA GeForce
RTX 2060. The retained route is
`artifacts/T022_visual_capture/{overview,ground_level,close_ground,return_trip}.png`
with `capture_report.json` and `godot_capture.log`. It shows the three
approved textured asset families over the streamed terrain; the return view
reproduces the overview pose without a whole-batch transition artifact.

## Unverified

Owner beauty acceptance of the complete procedural slice remains required for
T024. T022's asset contact, bounds, lifecycle, and safety behavior are
verified; long-route performance and final aesthetic judgment are not claimed.

## Review

Reviewed commits `016caa6` and the asset-integration follow-up. The renderer
is bounded to nine cell batches, updates on the main thread, rejects stale
epochs and invalid records, consumes the actual placement service, and loads
only approved local GLBs. The real-renderer route and five-test integration
suite pass. Accepted as DONE; T024 retains final owner beauty acceptance.
