# T023 evidence - consistent terrain materials and daylight

## Result

Added the checked-in `temperate_daylight_v1` preset. `ChunkTerrain` now
validates it before configuration, applies its material to every generated
chunk, and creates one shared WorldEnvironment and directional sun for the
streamed scene. The preset uses restrained woodland colour, bounded roughness
and UV repeat scale, fixed daylight, shadows, and light fog.

## Checks

| Command | Exit | Result |
| --- | ---: | --- |
| `python tools/check_game.py --suite visual_preset` | 0 | 5 executed, 5 passed, 0 failed |
| `python tools/check_game.py --suite landform` | 0 | 4 executed, 4 passed, 0 failed |
| `python tools/check_game.py --suite terrain` | 0 | 4 executed, 4 passed, 0 failed |
| `python tools/check_game.py --suite placement` | 0 | 6 executed, 6 passed, 0 failed |
| `python tools/check_game.py --suite vegetation` | 0 | 5 executed, 5 passed, 0 failed |
| `python tools/run_visual.py --output-dir artifacts/T023_visual_capture --timeout 180` | 0 | 4 real-renderer PNGs and report retained |

## Visual review

The Windows/OpenGL capture uses the NVIDIA GeForce RTX 2060 and the fixed
seed-42 route. `overview.png`, `ground_level.png`, and `close_ground.png` show
a muted green ground and consistent blue daylight; `return_trip.png` matches
the overview pose. The close view still exposes the simple heightmap surface
and sparse vegetation, so this is recorded for T024 rather than masked with
stronger fog.

Evidence: `artifacts/T023_visual_capture/{overview,ground_level,close_ground,return_trip}.png`,
`capture_report.json`, and `godot_capture.log`.

## Unverified

This task does not claim owner beauty acceptance, final landform richness, or
long-route performance. Those remain T024 concerns.

## Review

Implementation checks pass and the visible palette is more coherent than the
previous over-bright test green. Review the staged diff, the retained report,
and the fixed PNGs before promoting T023 to DONE.
