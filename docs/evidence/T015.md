# T015 evidence - repeatable GPU scene captures

## Environment

- Godot: `4.6.2-stable (official)`
- Platform/display: Windows, real `Windows` display server
- Renderer: `gl_compatibility`
- Video adapter: `NVIDIA GeForce RTX 2060`
- World: seed `42`, generator `g1`, content `c1`

## Checks

| Command | Result | Retained evidence |
| --- | --- | --- |
| `python -m unittest discover -s tools -p "test_*.py"` | PASS, 33 tests | terminal result |
| `python -m unittest tools/test_run_visual.py` | PASS, 3 tests | terminal result |
| `python tools/run_visual.py --output-dir artifacts/T015_visual_capture_final3` | PASS, 4/4 non-empty PNGs and report | `artifacts/T015_visual_capture_final3/godot_capture.log`, `capture_report.json`, `overview.png`, `ground_level.png`, `close_ground.png`, `return_trip.png` |
| Visual inspection of overview, ground-level, close-ground, and return-trip PNGs | PASS for readable terrain coverage; return-trip uses the identical fixed pose | `artifacts/T015_visual_capture_final2/*.png` |

## Implemented evidence

The wrapper is explicitly non-headless and records the actual renderer and
adapter. The capture scene waits for ChunkTerrain READY state before producing
images. Missing display, report, image, or empty output is rejected; beauty is
left to owner review.

Later terrain, lighting, material, or asset changes require recapture.

## Review status

The task is ready for review. Windows export remains unverified from T012 and
is not claimed by this capture task.
