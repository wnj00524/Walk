# T006 evidence - logical world positions

## Result

Implemented C01 logical positions: signed horizontal cells, 256-metre local
offsets, mathematical-floor handling across the origin, finite-value rejection,
and decimal-string cell serialisation for save consumers.

## Verification

Environment: Windows, Godot 4.6.2 candidate from `docs/toolchain.lock.json`,
Python 3.10+.

| Command | Exit code | Result |
| --- | ---: | --- |
| `python tools/check_game.py --suite coordinates` | 0 | 4 executed, 4 passed, 0 failed |
| `python tools/check_game.py --suite player` | 0 | 3 executed, 3 passed, 0 failed |
| `python tools/check_game.py --suite smoke` | 0 | 1 executed, 1 passed, 0 failed |
| `python tools/project.py validate` | 0 | PASS: 36 task records and plan structure checked |
| `python -m unittest discover -s tools -p "test_*.py"` | 0 | 30 tests pass |

The coordinate suite loads every example in
`fixtures/coordinate_vectors.json`, then checks repeated boundary crossings,
non-finite values, and invalid local offsets.

Retained focused logs: `artifacts/check_game_20260917-150403_import.log`,
`artifacts/check_game_20260917-150403_suite_coordinates.log`,
`artifacts/check_game_20260917-150411_suite_player.log`, and
`artifacts/check_game_20260917-150419_suite_smoke.log`.

## Unverified items

No terrain recentering, renderer precision, plugin integration, or Windows
export is claimed by this task. The player and smoke checks are headless.
