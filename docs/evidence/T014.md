# T014 evidence - deterministic terrain and seam regression tests

## Environment

- Godot: `4.6.2.stable.official.71f334935`
- Python: `3.12.12`
- Platform: Windows development host

## Checks

| Command | Result | Retained evidence |
| --- | --- | --- |
| `python tools/project.py validate` | PASS | terminal result |
| `python -m unittest discover -s tools -p "test_*.py"` | PASS, 30 tests | terminal result |
| `python tools/check_game.py --suite terrain` | PASS, 4/4 | `artifacts/check_game_20260917-211356_import.log`, `artifacts/check_game_20260917-211356_suite_terrain.log` |

## Coverage

The fixture pins the accepted seed-42 centre, negative-cell, and shared-edge
samples. The suite uses the public `ChunkTerrain` sampler, checks both paths
at the shared edge within 0.01 m, repeats generation, and confirms that a
changed seed changes the surface. A wrong offset or seed therefore breaks the
suite instead of being silently accepted.

Visual detail-level seam inspection remains a graphical review concern and is
not claimed by this headless suite.

## Review status

The task is ready for review. No production generator or world/save format was
changed.
