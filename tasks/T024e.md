# T024e - Tune habitat suitability for readable grouping

```json
{
  "id": "T024e",
  "role": "implementer",
  "depends_on": ["T022", "T023"],
  "read": [
    "docs/ART_DIRECTION.md",
    "docs/features/placement-rules.md",
    "game/data/habitats.json",
    "game/src/world/placement_rules.gd",
    "game/tests/test_placement_rules.gd"
  ],
  "write": [
    "game/data/habitats.json",
    "game/tests/test_placement_rules.gd"
  ]
}
```

Status is recorded only in PLAN.md.

## Read set

Read the root rules and recent log, then the art-direction and placement
walkthroughs and the actual habitat and placement-test files.

## Allowed changes

Only habitat suitability, cluster, and asset-scale metadata plus focused
placement-test coverage, and this task's evidence report, relevant walkthrough,
CODE_GUIDE.md, PLAN.md row, and one appended DLOG.md row. No new assets, paid
generation, terrain recipe, renderer, save, or camera-route changes.

## Outcome

Forest, meadow, and rocky records form visibly denser, grounded groups in the
fixed seed captures while remaining slope-aware, deterministic, and bounded.

## Acceptance

- Habitat slope/scale/grouping changes remain within existing validation limits.
- Placement remains deterministic, sorted, cell-owned, terrain-gated, and
  approved-asset-only.
- Fixed seed-42 and supplementary seed-1337 captures show readable grouping.
- Placement, vegetation, visual-preset, terrain, helper, and project checks are
  recorded with retained PNG evidence.

## Steps

1. Tune slope suitability, cluster spread, and asset scale within the existing
   schema limits; preserve habitat distinction.
2. Run focused placement and affected regressions.
3. Capture and inspect fixed-seed views, then record evidence.

## Non-goals

No new species, asset generation, terrain backend/recipe, renderer, save format,
or camera-route changes.

## Stop conditions

Stop after two unsuccessful focused tuning attempts or if terrain/backend or
new asset work is required. Apply AGENTS.md's two-repair rule.

## Close the session

Report result, changed files, actual checks, accessible evidence,
documentation, unverified items, and the next task. Do not start the next task.
