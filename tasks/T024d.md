# T024d - Add near-ground visual-gate viewpoints

```json
{
  "id": "T024d",
  "role": "implementer",
  "depends_on": ["T022", "T023"],
  "read": [
    "docs/ART_DIRECTION.md",
    "docs/features/visual-captures.md",
    "game/tests/visual_capture.gd",
    "game/tests/visual_route.json"
  ],
  "write": [
    "game/tests/visual_route.json"
  ]
}
```

Status is recorded only in PLAN.md.

## Read set

Read the root rules and recent log, then the visual-capture walkthrough and the
actual capture runner and route.

## Allowed changes

Only fixed-seed camera poses in `game/tests/visual_route.json`, plus this
task's evidence report, relevant walkthrough, CODE_GUIDE.md, PLAN.md row, and
one appended DLOG.md row. No terrain, placement, renderer, asset, or save
changes.

## Outcome

The fixed route includes a near-ground view that makes the approved 1–2 m
vegetation meshes and their deterministic groups readable in PNG evidence.

## Acceptance

- Seed 42 and the retained supplementary seed 1337 route views show grounded,
  readable vegetation without changing world generation.
- The route still contains overview, ground-level, close, and return poses.
- The real-renderer capture validates all named PNGs and report metadata.

## Steps

1. Adjust only camera positions and targets using the existing seed and recipe.
2. Capture and inspect all route PNGs.
3. Record the visual result and exact command evidence.

## Non-goals

No new assets, material changes, terrain, placement, renderer, or runtime API.

## Stop conditions

Stop if a source/runtime change is required or if the route cannot show the
approved meshes without masking quality defects. Apply AGENTS.md's two-repair rule.

## Close the session

Report result, changed files, actual checks, accessible evidence,
documentation, and the next task. Do not start the next task.
