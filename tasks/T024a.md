# T024a - Improve vegetation grouping for the visual gate

```json
{
  "id": "T024a",
  "role": "implementer",
  "depends_on": [
    "T022",
    "T023"
  ],
  "read": [
    "docs/ART_DIRECTION.md",
    "docs/features/placement-rules.md",
    "docs/features/vegetation-batches.md"
  ],
  "write": [
    "game/data/habitats.json",
    "game/tests/test_placement_rules.gd"
  ]
}
```

Status is recorded only in PLAN.md. This is a bounded revision task split from
the T024 owner gate; it does not accept T024 or change terrain generation.

## Outcome

The existing temperate forest, meadow, and rocky habitats read as intentional
groups at the fixed review viewpoints while retaining deterministic ownership
and the nine-batch renderer bound.

## Read set

Read the root rules/orientation/recent log, then the art-direction and
placement/vegetation walkthroughs and the actual habitat and placement test.

## Allowed changes

Only the habitat metadata and its focused placement-test coverage, plus this
task's evidence report, relevant walkthrough, CODE_GUIDE.md, PLAN.md row, and
one appended DLOG.md row. Do not change the terrain recipe, renderer, asset
catalogue, or save format.

## Steps

1. Tune density, cluster count, and scale ranges within the existing validated
   limits; keep habitats distinct and avoid uniform scattering.
2. Add an independent assertion for the intended bounded visual budget.
3. Re-run fixed real-renderer views and inspect ground contact and grouping.

## Acceptance

- Placement remains deterministic, sorted, cell-owned, slope-aware, and
  approved-asset-only.
- The fixed seed-42 and supplementary seed-1337 captures show more readable
  grouping without exceeding the renderer's nine resident batches.
- Run the placement, vegetation, visual-preset, terrain, helper, and project
  checks. Record exact commands and retained captures.

## Non-goals

No new species, paid asset generation, terrain changes, wind motion, or owner
beauty acceptance. T024 remains the visual-quality gate.

## Stop conditions

Stop for an asset-quality defect, a required terrain change, or a scope larger
than this data/test-sized revision. Apply AGENTS.md's two-repair rule.

## Close the session

Report result, changed files, actual checks, accessible evidence,
documentation, unverified items, and one next task. Do not start the next task.
